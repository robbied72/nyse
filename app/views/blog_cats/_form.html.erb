<%= form_for(@blog_cat) do |f| %>
  <% if @blog_cat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog_cat.errors.count, "error") %> prohibited this blog_cat from being saved:</h2>

      <ul>
      <% @blog_cat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
